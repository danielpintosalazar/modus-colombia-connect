# Decisiones técnicas

Stack fijo (no renegociable durante la hackatón, ver sección 3 de `PLAN_CLAUDE_CODE.md`): FastAPI, Firestore, Firebase Auth, Firebase Storage, Vertex AI (Gemini), Google Maps API, BigQuery (opcional), Docker.

Cualquier framework o servicio adicional se documenta aquí con el motivo antes de introducirlo.

## Registro

- **2026-08-27** — Carpeta `Back/` del repo (ya existente) se usa como `backend/` del plan, sin renombrar. `docs/`, `data/` y `docker-compose.yml` viven en la raíz del repo, junto a `Back/` y `frontend/`. Decisión tomada con el usuario al iniciar la Fase 0 para no reestructurar un repo ya en uso.
- **2026-08-27** — Los agentes (`diagnostico`, `priorizacion`, `orquestador`) se implementan con **fallback simulado basado en reglas primero**, y luego se integra Vertex AI (Gemini) por function calling sobre las mismas tools, tal como pide la Fase 2 del plan. El fallback no se elimina: queda como resiliencia (Fase 5) si Vertex AI falla en la demo.
- **2026-08-27** — Autenticación Firebase Admin SDK y llamadas a Vertex AI se escriben contra el SDK real desde el inicio (no solo como TODO), aunque no puedan probarse en esta máquina hasta completar el login manual de `gcloud`/`firebase` (ver `PROMPT_CONFIGURACION_INFRA(1).md`, Parte A — pendiente, no instalado en este entorno). El código falla de forma controlada (excepción capturada → fallback) si las credenciales no están configuradas, nunca con una excepción sin manejar.
- **2026-08-27** — `requirements.txt` usa versiones mínimas (`>=`) en vez de pines exactos: esta máquina tiene Python 3.14 (Fedora 44, sin 3.11 disponible) y pinear versiones exactas de todo el árbol de `google-cloud-aiplatform` hacía que el resolver de pip se colgara por horas intentando satisfacer combinaciones viejas incompatibles con cp314. `Dockerfile` sigue fijado a `python:3.11-slim` (la versión que pide el plan) para que el contenedor de cada miembro del equipo sea reproducible independientemente de qué Python tenga su máquina local.
- **2026-08-27** — Se agregó `pydantic[email]` (trae `email-validator`) a `requirements.txt`: `EmailStr` en `models/usuario.py` lo requiere en tiempo de import, no solo de validación, y faltaba en el `requirements.txt` inicial. Verificado con `python -c "from app.main import app"`, `pytest` (7/7 OK) y un arranque real de `uvicorn` con `/health` en 200 y `/zonas` en 401 sin token.
- **2026-08-27 (Infraestructura Parte B)** — Aprovisionamiento completo de Google Cloud y Firebase en el proyecto `go-fest-506814` (`us-central1`):
  - Creación de Firestore Native DB en `us-central1` y despliegue de `firestore.rules` con Firebase CLI.
  - Creación de bucket de almacenamiento `gs://go-fest-506814-storage`.
  - Service Account `firebase-adminsdk-hackaton@go-fest-506814.iam.gserviceaccount.com` configurada con roles `datastore.user`, `aiplatform.user`, `bigquery.admin`, `storage.admin` y `firebaseauth.admin`.
  - Creación del dataset de analítica `metricas_desastre` en BigQuery (Location: `US`).
  - Sembrado de 21 documentos iniciales de demo en Firestore (`usuarios`, `zonas`, `damnificados`, `donaciones`).
  - Resolución dinámica de credenciales en `app/core/firebase.py` y `app/core/vertex.py` para soportar `Certificate` con `Back/firebase-key.json` y fallback a `ApplicationDefault`.
- **2026-08-28 (Google Maps Platform & AGENTS.md)**:
  - Integración de **Google Maps JavaScript API** mediante `@googlemaps/js-api-loader` en [GoogleEmergencyMap.tsx](file:///c:/Users/DANIEL/Documents/hackaton/modus-colombia-connect/frontend/src/components/modus/GoogleEmergencyMap.tsx), reemplazando la imagen fija con un mapa interactivo geoespacial centrado en Colombia.
  - Estilo oscuro de alto contraste (*Slate / Navy / Emergency Red*) adaptado a la identidad visual del proyecto.
  - Soporte para alternar entre vista vectorial oscura y vista satelital híbrida para monitoreo de desastres.
  - Marcadores de emergencias con animación de alerta por severidad y despliegue de zonas de riesgo UNGRD/IDEAM con radios de influencia.
  - Establecimiento de [AGENTS.md](file:///c:/Users/DANIEL/Documents/hackaton/modus-colombia-connect/AGENTS.md) como guía maestra y regla de documentación continua para el asistente Antigravity.
- **2026-08-28 (rectificar_verificar.md — decisiones D1–D8)** — Aplicadas las decisiones pendientes del documento `rectificar verificar.md`:
  - **D1** — Estado y Entidad de Respuesta se **fusionan en un solo rol P0**: `estado_entidad_respuesta` (permisos combinados: panel consolidado + vincularse a necesidades + reportar avance). Cambios en `models/usuario.py`, `models/agentes.py`, `core/auth.py` (las claves de dev-token `estado`, `entidad_respuesta`, `usr-estado-1`, `usr-entidad-1` se conservan como **alias** del rol combinado), `api/{estado,entidades,damnificados}.py`, `agents/orquestador/{router,prompts}.py` y `firestore.rules`. Frontend: `RoleSwitcher` mantiene las pestañas "Gobierno" y "Respuesta" (ambas vistas ya construidas) — las dos mapean al rol único del backend. Separarlas en dos roles queda como P1.
  - **D2** — Confirmado stack Firebase/Firestore + FastAPI + Python (sin PostGIS ni C#). Firestore no consulta polígonos: las zonas usan `lat/lng + radio_km`; los polígonos, si se necesitan, se sirven como capa visual estática desde Storage.
  - **D3** — Nuevo esquema `models/iniciativa.py` (`Iniciativa`) — versión P0 mínima (sin fotos antes/después ni insights de inversión) + regla Firestore `iniciativas/{id}` (lectura autenticada).
  - **D4** — Nuevo esquema `models/necesidad.py` (`Necesidad`) con trazabilidad de origen (`fuente`: `manual | agente_diagnostico | sistema_riesgo`) + regla Firestore `necesidades/{id}` (solo `estado_entidad_respuesta`).
  - **D5** — Renombrado el segundo uso de "Acceso a Oportunidades" (conectar personas con ayudas/empleo/formación) → **"Redes de apoyo social"** en `frontend/src/lib/modus-data.ts` (`investmentTypes` e iniciativa INI-006). "Recuperación de Ingresos" (negocios afectados) se mantiene.
  - **D6** — El reporte de emergencia (`ReportEmergencyDialog.tsx`) queda en P0 con **solo texto + imagen + geolocalización automática**. Se eliminó la pestaña de nota de voz/grabación (audio/video con transcripción → P2).
  - **D7** — Top 10 de donantes y comparación público vs. privado quedan en **P1** (agregaciones simples sobre `donaciones`, sin BigQuery). Sin cambios de código ahora.
  - **D8** — Mapeo de materia prima para reconstrucción (madera / GeoCVC / *Pinus patula*) fuera de alcance: verificado que no existen modelos, agentes ni endpoints; solo nota de roadmap.
  - Verificación: `pytest` 7/7 OK, `from app.main import app` OK, `tsc --noEmit` OK, `npm run build` OK. Además se sincronizó `frontend/node_modules` con `package.json` (faltaban `@googlemaps/js-api-loader` y `@types/google.maps`, que rompían `tsc`/`build`).
- **2026-08-28 (D3/D4 — plumbing + fallbacks + auth dev en Docker)** — Cableado de las piezas que faltaban para la demo:
  - **Endpoints nuevos**: `api/iniciativas.py` (`GET /iniciativas/publicas` sin auth con fallback a `data/mock/iniciativas.json`; `GET /iniciativas` autenticado) y `api/necesidades.py` (`GET /necesidades` y `POST /necesidades` para rol `estado_entidad_respuesta`, con fallback de lectura a `data/mock/necesidades.json`). Registrados en `main.py`.
  - **`repositorio.py`**: `listar_iniciativas`, `listar_necesidades`, `crear_necesidad`. **`scripts/cargar_mock.py`**: siembra `iniciativas` y `necesidades`.
  - **`GET /metricas`**: ahora tiene fallback — si Firestore no está, calcula `deficit_por_sector` sobre `data/mock/{zonas,donaciones}.json` y marca `fuente: "demo"`. Nunca 503 en la demo.
  - **`core/auth.py`**: los `dev-token:<rol>` y el usuario de prueba por defecto se aceptan en `ENVIRONMENT ∈ {local, docker, dev, development}` (antes solo `local`), para que el contenedor de la demo pueda autenticar sin Firebase Auth real.
  - **`core/firebase.py`**: degradación limpia cuando el bind mount de `firebase-key.json` crea un directorio vacío — se verifica ADC real con `google.auth.default()` antes de intentar `initialize_app`, y se captura el `ValueError` de "app ya inicializada" (reload de uvicorn). Sin traceback ruidoso en logs.
  - **Frontend**: `modus-api.ts` añade `getPublicInitiatives()` (mapea `Iniciativa` backend → `Initiative` UI, fallback a mock), `getMetrics()` (→ KPIs) y `devAuthHeaders(rolUI)` (mapea rol de `RoleSwitcher` → `dev-token` del backend). `index.tsx` los carga en paralelo en el `loader` y `PublicPortal` los consume (grid de iniciativas + KPI "Personas afectadas"/"zonas críticas").
  - Verificación E2E en Docker: `pytest` 11/11, `tsc`/`build` OK, `GET /iniciativas/publicas`, `/necesidades/` (con `dev-token`), `/metricas/` → 200; la home SSR renderiza iniciativas del backend (p. ej. "Pozos comunitarios … La Guajira", que solo existe en `iniciativas.json`).
- **2026-08-28 (Firebase Auth — servicio con degradación limpia)** — Se conecta Firebase Auth de punta a punta, opt-in por config:
  - **Backend**: `api/auth.py` con `GET /auth/me` (devuelve el `Usuario` del token actual, sea ID token real o `dev-token`) y `GET /auth/config` (informa si el backend acepta dev-token). `core/auth.py`: si el ID token verificado no trae el custom claim `rol`, se intenta resolver desde `usuarios/{uid}` en Firestore antes de rechazar. `data/mock/usuarios.json`: los dos usuarios con rol antiguo (`usr-entidad-1`, `usr-estado-1`) pasan a `estado_entidad_respuesta` (bug latente de D1: el modelo `Usuario` ya no aceptaba los literales viejos). El script `scripts/crear_usuarios_auth.py` (ya existía) siembra los usuarios en Firebase Auth con su claim de rol (contraseña `Password123!`).
  - **Frontend**: `src/lib/firebase.ts` (init perezoso del SDK web, activo solo si hay `VITE_FIREBASE_API_KEY`+`AUTH_DOMAIN`+`PROJECT_ID` y estamos en el navegador). `src/lib/auth.tsx` (`AuthProvider` + `useAuth()`): escucha `onIdTokenChanged`, resuelve el rol vía `GET /auth/me`, y registra el proveedor de ID token en `modus-api.ts`. `modus-api.ts`: `authHeaders()` async → ID token real si hay sesión, si no `dev-token:<rol>` (comportamiento anterior); nuevo `getMe()`. `LoginDialog.tsx` (email/contraseña) integrado en el popover de `RoleSwitcher`. `index.tsx`: al iniciar sesión, la vista sigue al rol del claim (`roleKeyForBackendRole`). `AuthProvider` montado en `__root.tsx`.
  - **Fallback (Regla 2)**: sin `VITE_FIREBASE_*` el login no aparece y todo sigue con el toggle de rol local + `dev-token`. Verificado en Docker: `pytest` 13/13, `tsc`/`build` OK, `/auth/me` y `/auth/config` responden, la home SSR no rompe sin Firebase configurado.
  - **Pendiente (requiere proyecto Firebase real)**: poner las `VITE_FIREBASE_*` del proyecto `go-fest-506814` en `frontend/.env`, `Back/firebase-key.json`, y correr `python -m scripts.crear_usuarios_auth`. Recién ahí se prueba el flujo con ID tokens reales.
- **2026-08-28 (credenciales reales + capa de escritura + agentes en vivo)** — Con `Back/firebase-key.json` (SA `firebase-adminsdk-hackaton@go-fest-506814`) ya presente:
  - **Firestore, Cloud Storage y Vertex AI quedan EN VIVO.** Firestore sembrado con `scripts.cargar_mock` (32 docs, incl. `iniciativas` y `necesidades`).
  - **Modelo Gemini: `gemini-2.0-flash` → `gemini-2.5-flash`** en `.env`/`.env.example`/`config.py`. El proyecto `go-fest-506814` en `us-central1` no da acceso a `gemini-2.0-flash` ni a `1.5-*` (404 "model not found"); `gemini-2.5-flash` y `-flash-lite` sí responden.
  - **Bug de resiliencia corregido en `core/timeouts.py`**: `with ThreadPoolExecutor(...)` bloqueaba en su salida hasta que la tarea terminaba, anulando el timeout (una llamada a Gemini de 47 s devolvía el fallback… a los 47 s). Ahora `shutdown(wait=False, cancel_futures=True)`: el fallback vuelve al llegar al timeout y el hilo huérfano muere solo. `EXTERNAL_CALL_TIMEOUT_SECONDS` 8 → 12.
  - **Orquestador**: parseo robusto de la respuesta de Gemini (`_extraer_json_dict` quita fences ```json y prosa; `_texto_seguro` evita el crash cuando la última parte no es texto). Con esto el chat responde por Gemini (`modo=vertex_ai_gemini`) en ~9 s; la priorización vía function-calling sigue cayendo a reglas por tiempo (fallback determinista, correcto).
  - **Endpoints nuevos**: `POST /iniciativas` (publicar campaña, rol `estado_entidad_respuesta`), `PATCH /necesidades/{id}` (vincular entidad / cambiar estado), `POST /reportes/imagen` (sube a Cloud Storage y devuelve URL firmada de 7 días). Registrados en `main.py`. `repositorio.py`: `crear_iniciativa`, `obtener_necesidad`, `actualizar_necesidad`.
  - **`data/mock/usuarios.json`**: los roles `estado`/`entidad_respuesta` → `estado_entidad_respuesta` (D1).
  - **Frontend — capa de escritura en `modus-api.ts`**: `postChat`, `reportarDamnificado`, `crearDonacion`, `getMisDonaciones`, `getNecesidades`, `vincularNecesidad`, `actualizarEstadoNecesidad`, `crearIniciativa`, `getOrdenesDespliegue`, `priorizarRecursos`, `subirImagenReporte` (multipart). Helper `apiSend` con timeout de 20 s para operaciones que tocan agentes.
  - **Frontend — vistas cableadas**: `OrquestadorChat.tsx` (nuevo, chat con el Orquestador, montado en el portal público), `ReportEmergencyDialog` (sube imagen real a Storage + `POST /damnificados`), `VictimLightView` (`POST /damnificados`). Todas conservan `toast` de fallback si la llamada falla (Regla 2).
  - Verificación E2E en Docker: `pytest` 13/13; `POST /damnificados`, `/iniciativas`, `PATCH /necesidades`, `POST /reportes/imagen` (archivo real en `gs://go-fest-506814-storage/reportes/…`), `POST /chat` con `modo=vertex_ai_gemini`; `tsc`/`build` OK.
  - **Pendiente**:
    - **Habilitar Authentication en la consola** (Firebase Console → Authentication → Get started → Email/Password). `scripts.crear_usuarios_auth` falla con `CONFIGURATION_NOT_FOUND` hasta entonces.
    - **Vistas aún en mock**: `GovDonorView`, `ResponseEntityView`, `PrivateDonorView`, `InitiativeDetailDialog`. Las funciones de `modus-api.ts` ya existen; falta conectarlas a los botones.
- **2026-08-28 (chat contextual + vistas por rol cableadas)** —
  - **Chat único que detecta contexto**: `OrquestadorChat` ya no recibe un rol fijo. `inferIntent(mensaje)` clasifica el texto en `damnificado` ("se derrumbó mi casa", "estoy atrapado", "necesito"…) o `donante` ("quiero ayudar", "donar", "aportar"…) por regex, elige el `roleKey` correspondiente y muestra un badge "Modo afectado / Modo donante". El backend `/chat` ya adapta la respuesta al rol. Verificado: mismo componente, "se derrumbó mi casa en Mocoa" → respuesta empática de registro de necesidades; "quiero donar agua" → respuesta de priorización de aporte. Ambas por Gemini.
  - **`ResponseEntityView`**: bloque nuevo "Necesidades registradas en el sistema" que lee `/necesidades` real (con badge de `fuente`), con acciones **Vincularme** (`PATCH …/{id}` `vincular`) y **Marcar cubierta** (`PATCH` `estado`). Formulario "Crear campaña" → `POST /iniciativas` (con selects de zona/sector y población).
  - **`InitiativeDetailDialog`**: "Invertir / Donar" con campo de monto → `POST /donaciones` (sector derivado del tipo de inversión, zona del `region`). Toast con el id/estado real de la donación.
  - **`PrivateDonorView`**: catálogo alimentado por `getPublicInitiatives()` (fallback a mock); "Invertir / Donar" abre el detalle (que hace la donación real); sección "Mis aportes registrados" desde `GET /donaciones/mias`.
  - **`GovDonorView`**: sección nueva "Déficit por sector y priorización" — lista `deficitPorSector` de `/metricas` (con `fuente`), botón **"Priorizar recursos con IA"** → `POST /agentes/priorizacion` (Gemini con function-calling; cae a reglas deterministas por tiempo, resultado igualmente coherente) y muestra las órdenes; carga inicial de `/estado/ordenes-despliegue`.
  - Verificación E2E en Docker: `pytest` 13/13, `tsc`/`eslint`/`build` OK; `POST /donaciones` + `GET /donaciones/mias`, `PATCH /necesidades`, `POST /iniciativas`, `/chat` con detección de intención — todo 200 contra Firestore/Gemini reales.
- **2026-08-28 (ampliación de alcance: operaciones de campo)** — Nuevo dominio `models/operaciones.py` (`AvanceCampo`, `NotificacionEntidad`, `CentroAcopio`) con sus colecciones Firestore y reglas:
  - **Avances de campo**: `POST /avances` + `GET /avances` (rol `estado_entidad_respuesta`). Si el avance referencia una iniciativa y trae `progreso_pct`, el backend **actualiza `iniciativas/{id}.progreso`** en cascada (verificado: reportar 75% deja la iniciativa en `0.75`, visible en `/iniciativas/publicas`).
  - **Notificaciones a entidades**: `POST /estado/notificaciones` + `GET /estado/notificaciones` (participación requerida; estado `enviada|aceptada|rechazada`).
  - **Centros de acopio**: `GET /centros-acopio` (lectura autenticada, fallback a `data/mock/centros_acopio.json`) + `POST /centros-acopio` (alta, rol `estado_entidad_respuesta`). Sembrados 3 centros.
  - `repositorio.py` y `scripts/cargar_mock.py` extendidos; `firestore.rules` +3 colecciones.
  - **Frontend**: `modus-api.ts` — `crearAvanceCampo`/`getAvances`, `getCentrosAcopio`/`crearCentroAcopio`, `notificarEntidad`/`getNotificacionesEntidad`.
    - `ResponseEntityView`: el "Reporte de campo" ahora sube la evidencia a Storage (`subirImagenReporte`) y llama a `POST /avances` con la iniciativa seleccionada (lista real desde `/iniciativas/publicas`).
    - `GovDonorView`: "Notificar Participación Requerida" → `POST /estado/notificaciones` (+ tabla de notificaciones emitidas); el formulario de centros de acopio → `POST /centros-acopio` y la lista se alimenta de `GET /centros-acopio`.
  - Verificación E2E en Docker: `pytest` 15/15, `tsc`/`eslint`/`build` OK; `POST /avances` (con cascada), `POST /centros-acopio`, `POST /estado/notificaciones` → todo 201 contra Firestore real.
- **2026-08-28 (fase de análisis: modelo de evidencias visuales)** — Base para trabajar con imágenes reales de zonas afectadas:
  - `models/evidencia.py` (`Evidencia`, `DiagnosticoEvidencia`): imagen en Cloud Storage + `zona_id`, `tipo` (`satelital_antes|satelital_despues|aerea|dron|terreno`), **procedencia y licencia obligatorias** (`fuente`, `licencia`, `fecha_captura`), y la clasificación del Agente de Diagnóstico si se ejecutó.
  - `services/storage.py`: helper `subir_imagen(contenido, content_type, prefijo) -> (gs_uri, url_firmada_7d)`, extraído de `reportes.py` (que ahora lo reutiliza). El `gs://` URI queda listo para pasárselo a Gemini multimodal.
  - `api/evidencias.py`: `POST /zonas/{zona_id}/evidencias` (multipart: archivo + tipo/fuente/licencia/fecha + `ejecutar_diagnostico`; sube a Storage, registra en Firestore y corre el diagnóstico best-effort), `GET /zonas/{zona_id}/evidencias` (autenticado), `GET /evidencias` (rol `estado_entidad_respuesta`), `POST /evidencias/{id}/diagnostico` (re-clasificar).
  - `repositorio.py` +CRUD `evidencias`; `firestore.rules` +colección `evidencias` (lectura autenticada, escritura solo backend).
  - Verificación E2E: subida real a `gs://go-fest-506814-storage/evidencias/zona-mocoa/…`, metadatos + clasificación persistidos (`clasificacion=destruida`, `modo=vertex_ai_gemini`), GET y re-diagnóstico OK. `pytest` 16/16.
  - **Pendiente (fase de imágenes)**: (1) upgrade del Agente de Diagnóstico a **visión multimodal real** — hoy Gemini razona sobre la URL como texto; falta pasar la imagen como `Part.from_uri(gs_uri, "image/jpeg")`. (2) `scripts/cargar_evidencias.py` para carga por lotes desde `data/evidencias/<zona>/manifest.json`. (3) curaduría de imágenes reales con licencia abierta (Copernicus EMS, Sentinel-2, OpenAerialMap).
- **2026-08-28 (buscador de imágenes reales)** — Sistema para encontrar y registrar imágenes de zonas afectadas:
  - `services/busqueda_imagenes.py`: proveedores **Wikimedia Commons** y **Openverse** (sin clave, licencia abierta) y **Google Programmable Search** opcional (`GOOGLE_SEARCH_API_KEY` + `GOOGLE_CSE_ID` en config/`.env.example`). Salida normalizada `ResultadoBusqueda` (url, thumbnail, título, fuente, licencia, autor, ficha). Llamadas HTTP acotadas por `run_with_timeout`.
  - `api/evidencias.py`: `GET /evidencias/buscar?q=&fuente=&limite=` (candidatos, no guarda) y `POST /zonas/{zona_id}/evidencias/desde-url` (registra una URL externa como `Evidencia` con `origen="url_externa"`, `gs_uri=""`, conservando fuente/licencia/autor/ficha y corriendo el diagnóstico). `Evidencia.gs_uri` pasa a opcional; nuevo campo `origen` (`subida|url_externa`).
  - **Frontend**: `modus-api.ts` — `buscarImagenes`, `registrarEvidenciaDesdeUrl`, `getEvidenciasDeZona`, `rediagnosticarEvidencia`. Componente `EvidenciaAnalisis.tsx` (montado en `ResponseEntityView`): selector de zona/fuente/tipo + búsqueda → grid de resultados con thumbnail y licencia → "Guardar + diagnosticar" → lista de evidencias registradas con badge de clasificación IA y "Re-diagnosticar".
  - Verificación E2E en Docker: `GET /evidencias/buscar` (Wikimedia) devuelve imágenes reales de dominio público (p. ej. "2017 Mocoa disaster aftermath"); `POST …/desde-url` → 201 con `origen=url_externa` y diagnóstico. `pytest` 16/16, `tsc`/`eslint`/`build` OK. (Openverse a veces agota el timeout desde este entorno de red → 502 controlado; Wikimedia es el proveedor por defecto.)
- **2026-08-28 (Sistema de Identificación: búsqueda y caché de desastres)** — El Sistema de Identificación ahora ingiere eventos de emergencia desde fuentes abiertas (noticias) y los deja listos para generar `necesidad` con `fuente="sistema_riesgo"`:
  - **`services/busqueda_desastres.py`**: usa **Google Programmable Search** como búsqueda web (no de imágenes), reutilizando `GOOGLE_SEARCH_API_KEY` + `GOOGLE_CSE_ID` (o `GOOGLE_SEARCH_CREDENTIALS`) y el mint OAuth2 de `busqueda_imagenes.py`. Consulta por defecto: emergencias en Colombia con damnificados; params `gl=co`, `lr=lang_es`, `sort=date`, `dateRestrict=dN`. Salida normalizada `Desastre` con clasificación heurística de `tipo` (`sismo|inundacion|deslizamiento|incendio|vendaval|erupcion|otro`) y `lugar` (regex de departamentos/ciudades). Acotada por `run_with_timeout`; si falla o no está configurada lanza `RuntimeError`.
  - **`models/desastre.py`**: nuevo modelo. Campo clave **`buscado_en`** (hora de la búsqueda que trajo el evento, distinta de `publicado`, la fecha del hecho). `id` = `des-<sha1(url)[:12]>`, así que re-buscar el mismo evento **actualiza su `buscado_en` en vez de duplicarlo**.
  - **`api/desastres.py`**: `GET /desastres/` (rol `estado_entidad_respuesta`), params `q`, `limite`, `dias`, `forzar`. **Caché de 12 h**: si ya existe **al menos un** desastre con `buscado_en` dentro de la ventana (`BUSQUEDA_DESASTRES_VENTANA_HORAS`, default 12), **no se llama a la API** y se devuelve lo cacheado (`fuente="cache"`). Si toca buscar, persiste el lote con `guardar_desastres` y responde `fuente="busqueda"`. Resiliencia (Regla 2): sin Firestore o con la búsqueda caída cae a caché y, en último caso, a `data/mock/desastres.json` (`fuente="demo"`) — nunca 5xx. `forzar=true` salta la caché para la demo.
  - **`repositorio.py`**: `listar_desastres()` / `guardar_desastres()` (upsert en lote por id). El filtro por ventana se hace en el endpoint (colección pequeña: ≤10 docs por búsqueda, reescritos por id).
  - **`firestore.rules`**: +colección `desastres` (lectura `estado_entidad_respuesta`, escritura solo backend). **`scripts/cargar_mock.py`** y `data/mock/desastres.json` (3 eventos con `buscado_en` antiguo → la 1ª llamada real siempre busca). **`config.py`/`.env.example`**: `BUSQUEDA_DESASTRES_VENTANA_HORAS=12`.
  - Verificación: `pytest` 19/19 (nuevos: rol requerido, fallback a demo sin Firestore ni búsqueda, uso de caché cuando hay un evento reciente).
  - **Pendiente**: (1) job/endpoint que convierta `Desastre` → `Necesidad` (`fuente="sistema_riesgo"`, `zona_id` por match de `lugar`); (2) cablear `getDesastres` en `modus-api.ts` + panel en `ResponseEntityView`.



