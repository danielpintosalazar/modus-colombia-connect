{
  "name": "modus-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd frontend && bun install",
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build && cd .. && rm -rf dist && cp -R frontend/dist dist",
    "build:dev": "cd frontend && bun run build:dev && cd .. && rm -rf dist && cp -R frontend/dist dist",
    "preview": "cd frontend && bun run preview",
    "lint": "cd frontend && bun run lint"
  }
}
